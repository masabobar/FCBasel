# Screen Map — {{PROJECT_NAME}}

**Version:** {{VERSION}}
**Last Updated:** {{DATE}}
**Last Refreshed By `/screen-map`:** {{DATE}}
**Status:** {{STATUS}}

> Hand-curated: navigation hierarchy, screen metadata (Name, Type, Path, Auth, Description), story back-links.
> Generated by `/screen-map`: API Endpoints columns + Status column. **Do not hand-edit the generated columns** — edit the linked story instead.

---

## 1. Navigation Hierarchy

Mark every node with its auth requirement: `(public)`, `(requireAuth)`, or `(requireRole=<ROLE>)`.

### {{NAV_GROUP_NAME}} (e.g. "Mobile App" / "Customer Web" / "Admin Panel")

```
{{ROOT_NAVIGATOR}}
├── {{STACK_OR_GROUP_1}}                      (requireAuth)
│   ├── {{SCREEN_1_NAME}}                     SCREEN-001
│   ├── {{SCREEN_2_NAME}}                     SCREEN-002
│   └── {{SCREEN_3_NAME}}                     SCREEN-003
└── {{STACK_OR_GROUP_2}}                      (public)
    ├── {{SCREEN_4_NAME}}                     SCREEN-004
    └── {{SCREEN_5_NAME}}                     SCREEN-005
```

> Duplicate this block per top-level navigator if the project has multiple (e.g. one app for customers, another for admins).

---

## 2. Screen Registry

### SCREEN-001 — {{Screen Name}}

| Field | Value |
|-------|-------|
| **Type** | Web / Mobile / Both |
| **Path** | `/products/:id` (web) — or `ProductDetailScreen` (mobile) |
| **Auth** | Public / Authenticated / Role: ADMIN |
| **Stories** | US-042, US-043 |
| **Status** | *(generated)* In Progress (1/2 completed) |

**Description:** {{One-to-two-sentence description of what the user does on this screen.}}

**API Endpoints Used:** *(generated by `/screen-map` from linked stories)*

| Method | Path | Purpose | Source story |
|--------|------|---------|--------------|
| GET    | /api/v1/products/:id | Load product detail | US-042 |
| POST   | /api/v1/cart/items   | Add to cart         | US-043 |

---

### SCREEN-002 — {{Screen Name}}

| Field | Value |
|-------|-------|
| **Type** | Web / Mobile / Both |
| **Path** | `{{PATH}}` |
| **Auth** | {{AUTH}} |
| **Stories** | {{STORY_IDS}} |
| **Status** | *(generated)* {{STATUS}} |

**Description:** {{Description}}

**API Endpoints Used:** *(generated)*

| Method | Path | Purpose | Source story |
|--------|------|---------|--------------|
| {{METHOD}} | {{PATH}} | {{PURPOSE}} | {{STORY_ID}} |

---

> Duplicate the screen-entry block above for every screen in the project. Keep entries in the same order as the navigation hierarchy (§1) for easy scanning.

---

## 3. Removed Screens

> When a screen is retired, move its entry here (do not delete) — preserves history for change discussions.

### SCREEN-XXX — {{Removed Screen Name}}
- **Removed on:** {{DATE}}
- **Reason:** {{Why removed — design pivot, feature cut, etc.}}
- **Was implemented by stories:** {{STORY_IDS}}

---

## 4. Drift Report (last `/screen-map` run)

> Auto-populated by `/screen-map`. Lists any inconsistencies between this file and the backlog stories. Resolve before the next sprint review.

- **Stories referencing screens not in this map:** *(none)*
- **Screen entries with no backing story (orphans):** *(none)*
- **Navigation nodes missing a registry entry (or vice versa):** *(none)*

---

**Rule:** `.claude/rules/screen-inventory.md`
**Refresh:** run `/screen-map` after any story add/edit/remove that touches a frontend (Type: Frontend) story.
**Source for API columns:** the `**API Endpoints Used:**` table in each story (per `.claude/rules/screen-driven-backlog.md`).
